[project]
name = "chat-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Richang Sharma", email = "richangsharma@toppanmerrill.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.13.5",
]

[project.scripts]
chat-api = "chat_api:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
